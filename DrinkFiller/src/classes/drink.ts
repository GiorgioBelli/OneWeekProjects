export class Drink {

    name: string;
    color: string;

    constructor(name: string, color: string) {
        this.name = name;
        this.color = color; // hex including the '#' or know color names (yellow, red ...)
    }
}